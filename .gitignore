__pycache__/
*.pyc

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
CLAUDE.md

# Internal development/handoff docs + reference assets — kept local, not in-repo
OPTION_B_HANDOFF.md
LINEUP_POPUP_HANDOFF.md
screenshot-atl-batters.png
# Raw screenshot sources used to produce docs/screenshots/*.webp. The
# cropped/optimized WebP versions go in docs/screenshots/ (committed); the
# originals stay local to keep the repo small. Leading slash anchors the
# pattern to repo root so docs/screenshots/ is *not* matched.
/screenshots/

# Local virtualenvs
.venv/
venv/

# JS dev tooling (added when JS test harness lands)
node_modules/
